# Copier ce fichier en .env et remplir les valeurs
# Ne jamais committer .env dans git

# Cle API secrete — generee aleatoirement (ex: openssl rand -hex 32)
API_KEY=your-super-secret-api-key-here

# Optionnel : surcharger le handle Kaggle si le modele est heberge ailleurs
# KAGGLE_MODEL_HANDLE=parfaitbotchi1/mnist-efficientnetv2s/pyTorch/default/1
